<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>com.eventhandler</groupId>
    <artifactId>event-handler-core</artifactId>
    <packaging>jar</packaging>
    <version>${parent.version}</version>
    <name>event-handler-core</name>

    <parent>
        <groupId>com.eventhandler</groupId>
        <artifactId>event-handler-parent</artifactId>
        <version>1.0-SNAPSHOT</version>
    </parent>

    <dependencies>
        <dependency>
            <groupId>com.eventhandler</groupId>
            <artifactId>event-handler-common</artifactId>
            <version>${parent.version}</version>
        </dependency>

        <dependency>
            <groupId>com.hazelcast</groupId>
            <artifactId>hazelcast-spring</artifactId>
            <version>3.5</version>
        </dependency>

        <dependency>
            <groupId>ru.yandex.qatools</groupId>
            <artifactId>yatomata</artifactId>
            <version>1.7</version>
        </dependency>
    </dependencies>
</project>
